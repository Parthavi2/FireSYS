PORT=5000
NODE_ENV=development

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=shraddha
DB_NAME=fire_dispatch

# Auth
JWT_SECRET=change_this_to_a_long_random_string
JWT_EXPIRES_IN=8h
# In production this becomes a real email send; for now the reset
# link/token is logged to the server console instead.
RESET_TOKEN_TTL_MINUTES=30
